data/
*.db
.env
JWT_SECRET
server.bak

# web frontend
web/node_modules/
web/dist/

# go vendor（本地依赖，go mod vendor 生成，不入库）
vendor/

# release build artifacts
dist/
*.tar.gz
*.zip
*.exe
*.app

# electron client
electron/node_modules/
electron/dist/
electron/out/

# dev container external Linux CLI binaries (k3d, kubectl, helm, etc.)
/bin/*
.bin/

# tools & system files
.DS_Store
.superpowers/
.zcode/
.qoder/
config.yaml.bak

# build artifacts (binaries)
# 锚定到根目录：否则 server 会匹配 cmd/server/、registry.json 会匹配 internal/acp/registry.json
/opennexus
/opennexus.log
/opennexus.pid
/server
/registry.json
/gateway

# ACP 报文实验产物（探针脚本与抓包数据）
.acp-experiment/

# git worktrees
.worktrees/

# temporary files
tasks.json
tmp_*.png
